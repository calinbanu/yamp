[package]
name = "parser"
version = "0.1.0"
authors = ["Banu Calin <banucaliniulian@gmail.com>"]
edition = "2018"

[dependencies]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
